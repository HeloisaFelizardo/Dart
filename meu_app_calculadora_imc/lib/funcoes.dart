// Esta função calcula o Índice de Massa Corporal (IMC) com base no peso e na altura fornecidos.
double calcularIMC(double peso, double altura) {
  // A fórmula do IMC é peso dividido pela altura ao quadrado.
  // Retorna o valor calculado como resultado.
  return peso / (altura * altura);
}
